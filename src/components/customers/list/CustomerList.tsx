import React, { Component } from 'react';

export class CustomerList extends Component {

    render(): React.ReactNode {
        return (
            <div>
                Customers list
            </div>
        );
    }
}